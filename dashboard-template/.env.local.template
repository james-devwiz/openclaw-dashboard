# OpenClaw Command Centre Configuration

# OpenClaw Gateway (via SSH tunnel)
OPENCLAW_GATEWAY_URL=http://localhost:18789
OPENCLAW_GATEWAY_TOKEN=your-dashboard-token-here

# Dashboard API authentication (bearer token for all /api/* routes)
DASHBOARD_API_TOKEN=generate-a-random-token-here
NEXT_PUBLIC_DASHBOARD_API_TOKEN=same-token-as-above

# Notion API (for non-task integrations)
NOTION_API_KEY=your-notion-api-key

# LinkedIn Intelligence (via Unipile) — optional
UNIPILE_DSN=https://api.unipile.com
UNIPILE_API_KEY=your-unipile-api-key
UNIPILE_ACCOUNT_ID=your-unipile-account-id

# Lead Enrichment (Apollo.io) — optional
APOLLO_API_KEY=your-apollo-api-key

# Email Verification (ZeroBounce) — optional
ZEROBOUNCE_API_KEY=your-zerobounce-api-key

# LinkedIn self-filtering — your LinkedIn profile identifier
LINKEDIN_OWN_IDENTIFIER=your-linkedin-identifier

# VPS SSH (for remote health checks)
VPS_IP=YOUR_VPS_IP
VPS_USER=root

# Path overrides (defaults are sensible — only set if non-standard)
# OPENCLAW_WORKSPACE_PATH=/root/.openclaw/workspace
# OPENCLAW_CONFIG_PATH=/root/.openclaw/openclaw.json
# OPENCLAW_CRON_PATH=/root/.openclaw/cron/jobs.json
# AUTH_PROFILES_PATH=/root/.openclaw/agents/main/agent/auth-profiles.json
